# Copy this file to .env and fill in sensible values

ADMIN_USERNAME=admin     # required; no default is used by the app
# generate bcrypt hash for your password, e.g.:
#   node -e "console.log(require('bcrypt').hashSync('yourpass', 10))"
ADMIN_PASSWORD_HASH=       # required

SESSION_SECRET=replace_with_a_long_random_string  # required, used to sign session cookies
# optional settings
PORT=8080
NODE_ENV=development
